# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Otimizado para o nível gratuito do Cloud Run (plano Blaze)
  cpu: 1
  memoryMiB: 512
  maxInstances: 1
  minInstances: 0
  concurrency: 80
